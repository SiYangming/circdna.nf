{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/circdna/master/assets/schema_input.json",
    "title": "nf-core/circdna pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces"
            },
            "fastq_1": {
                "type": "string",
                "pattern": "^\\S+\\.f(ast)?q\\.gz$",
                "errorMessage": "FastQ file for reads 1 must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'"
            },
            "fastq_2": {
                "errorMessage": "FastQ file for reads 2 cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'",
                "anyOf": [
                    {
                        "type": "string",
                        "pattern": "^\\S+\\.f(ast)?q\\.gz$"
                    },
                    {
                        "type": "string",
                        "maxLength": 0
                    }
                ]
            },
            "lane": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Lane value cannot contain spaces",
                "help_text": "Optional lane identifier used to distinguish multiple sequencing lanes for the same sample."
            },
            "datatype": {
                "type": "string",
                "enum": ["gdna", "eccdna"],
                "errorMessage": "Datatype must be one of: gdna, eccdna",
                "help_text": "Sample datatype. Required for Integrated Mode (mode=integrated) to distinguish gDNA (background) from eccDNA samples."
            },
            "platform": {
                "type": "string",
                "enum": ["illumina", "pacbio", "ont"],
                "errorMessage": "Platform must be one of: illumina, pacbio, ont",
                "help_text": "Sequencing platform."
            },
            "protocol": {
                "type": "string",
                "enum": ["short_read", "long_read"],
                "errorMessage": "Protocol must be one of: short_read, long_read",
                "help_text": "Sequencing protocol."
            }
        },
        "required": ["sample", "fastq_1"]
    }
}
