# 2026-06-24 模块替换记录

## 执行摘要

本次更新将 `/Users/siyangming/nextflow_nf_core/circdna.nf/modules` 目录中所有存在于 nf-core 标准库的模块替换为最新版本。所有可替换的模块都已更新完成，同时保留了必要的自定义模块。

## 备份信息

- **备份方式**: Git 版本控制
- **备份状态**: ✅ 已通过 Git 提交记录变更
- **备份内容**: 完整的 modules 目录变更历史，可通过 Git 查看替换前的所有文件

## 一、local 目录模块替换

### 1.1 已删除的 local 模块（6个）

| 旧模块路径 | 删除原因 | 替换位置 |
|-----------|---------|---------|
| `modules/local/circexplorer2/parse/` | nf-core 标准库存在 | `modules/nf-core/circexplorer2/parse/` |
| `modules/local/samblaster/` | nf-core 标准库存在 | `modules/nf-core/samblaster/` |
| `modules/local/cnvkit/batch/` | nf-core 标准库存在 | `modules/nf-core/cnvkit/batch/` |
| `modules/local/cnvkit/segment/` | nf-core 标准库存在 | `modules/nf-core/cnvkit/segment/` |
| `modules/local/seqtk/seq/` | nf-core 标准库存在 | `modules/nf-core/seqtk/seq/` |
| `modules/local/unicycler/` | nf-core 标准库存在 | `modules/nf-core/unicycler/` |

### 1.2 保留的 local 模块（6个）

| 保留模块路径 | 保留原因 |
|-------------|---------|
| `modules/local/ampliconsuite/` | nf-core 标准库中不存在 |
| `modules/local/bedtools/sortedbam2bed/` | nf-core 标准库中不存在 |
| `modules/local/bedtools/splitbam2bed/` | nf-core 标准库中不存在 |
| `modules/local/circlefinder/` | nf-core 标准库中不存在 |
| `modules/local/circlemap/readextractor/` | nf-core 标准库中不存在 |
| `modules/local/circlemap/realign/` | nf-core 标准库中不存在 |
| `modules/local/circlemap/repeats/` | nf-core 标准库中不存在 |
| `modules/local/getcircularreads/` | nf-core 标准库中不存在 |
| `modules/local/samplesheet_check/` | nf-core 标准库中不存在 |

## 二、nf-core 目录模块替换

### 2.1 完全替换的模块（14个）

| 模块 | 替换状态 | 新增子模块数量 | 主要改进 |
|------|---------|--------------|---------|
| `nf-core/bwa/` | ✅ 完整替换 | 5个 (aln, index, mem, sampe, samse) | BWA 0.7.17 → 0.7.19, Samtools 1.16.1 → 1.22.1 |
| `nf-core/cat/` | ✅ 完整替换 | 2个 (cat, fastq) | 标准化结构 |
| `nf-core/circexplorer2/` | ✅ 完整替换 | 2个 (annotate, parse) | 支持多种比对器自动检测 |
| `nf-core/cnvkit/` | ✅ 完整替换 | 11个 (access, antitarget, batch, call, coverage, export, fix, genemetrics, reference, segment, target) | 版本更新，功能增强 |
| `nf-core/custom/` | ✅ 完整替换 | 20+个工具 | 完整的自定义工具集 |
| `nf-core/fastqc/` | ✅ 完整替换 | - | 最新版本 |
| `nf-core/minimap2/` | ✅ 完整替换 | 2个 (align, index) | 最新版本 |
| `nf-core/multiqc/` | ✅ 完整替换 | - | 版本 1.18/1.19 → 1.35 |
| `nf-core/picard/` | ✅ 完整替换 | 20+个工具 | 完整的 Picard 工具集 |
| `nf-core/samtools/` | ✅ 完整替换 | 30+个工具 | Samtools 1.18 → 1.23.1 |
| `nf-core/seqtk/` | ✅ 完整替换 | 8个工具 | 完整的 Seqtk 工具集 |
| `nf-core/trimgalore/` | ✅ 完整替换 | - | 最新版本 |
| `nf-core/samblaster/` | ✅ 新增 | - | 从 local 迁移 |
| `nf-core/unicycler/` | ✅ 新增 | - | 从 local 迁移 |

## 三、主要改进

### 3.1 软件版本升级

| 工具 | 旧版本 | 新版本 | 改进内容 |
|------|--------|--------|---------|
| BWA | 0.7.17 | 0.7.19 | Bug 修复，性能优化 |
| Samtools | 1.16.1/1.18 | 1.22.1/1.23.1 | 新增功能，CRAM 改进 |
| MultiQC | 1.18/1.19 | 1.35 | 动态输入，自定义配置，logo 支持 |
| HTSlib | 未包含 | 1.22.1 | 完整的依赖链 |

### 3.2 功能增强

#### BWA_MEM
- ✅ 支持 BAM/CRAM/SAM 多种输出格式
- ✅ 支持 CRAM 输出的参考基因组输入
- ✅ 更灵活的参数配置

#### Samtools 工具
- ✅ 支持 BAI/CSI/CRAI 多种索引格式
- ✅ 支持 CRAM 输出和参考基因组
- ✅ 更完善的错误处理

#### MultiQC
- ✅ 动态输入路径支持
- ✅ 自定义 logo 和配置文件支持
- ✅ 样本名替换功能
- ✅ 更好的模块化设计

#### CIRCEXPLORER2_PARSE
- ✅ 支持多种比对器自动检测 (STAR/MapSplice/BWA/segemehl)
- ✅ 更好的参数配置

### 3.3 容器格式

- 部分模块采用 Seqera Wave 格式
- 保持 Galaxy/quay.io 容器以降低维护成本

### 3.4 测试覆盖

- 所有替换的模块都包含完整的 nf-test 测试用例
- 提供多种测试配置文件
- 包含 `.snap` 文件用于结果验证

## 四、替换后的模块结构

### 4.1 local 目录（6个自定义模块）

```
modules/local/
├── ampliconsuite/
│   ├── Dockerfile
│   ├── environment.yml
│   ├── main.nf
│   └── meta.yml
├── bedtools/
│   ├── sortedbam2bed/
│   │   ├── environment.yml
│   │   ├── main.nf
│   │   └── meta.yml
│   └── splitbam2bed/
│       ├── environment.yml
│       ├── main.nf
│       └── meta.yml
├── circlefinder/
│   ├── environment.yml
│   ├── main.nf
│   └── meta.yml
├── circlemap/
│   ├── readextractor/
│   │   ├── environment.yml
│   │   ├── main.nf
│   │   └── meta.yml
│   ├── realign/
│   │   ├── environment.yml
│   │   ├── main.nf
│   │   └── meta.yml
│   └── repeats/
│       ├── environment.yml
│       ├── main.nf
│       └── meta.yml
├── getcircularreads/
│   ├── environment.yml
│   ├── main.nf
│   └── meta.yml
└── samplesheet_check/
    ├── environment.yml
    ├── main.nf
    └── meta.yml
```

### 4.2 nf-core 目录（14个标准模块）

```
modules/nf-core/
├── bwa/ (5个子模块: aln, index, mem, sampe, samse)
├── cat/ (2个子模块: cat, fastq)
├── circexplorer2/ (2个子模块: annotate, parse)
├── cnvkit/ (11个子模块)
├── custom/ (20+个子模块)
├── fastqc/
├── minimap2/ (2个子模块: align, index)
├── multiqc/
├── picard/ (20+个子模块)
├── samtools/ (30+个子模块)
├── seqtk/ (8个子模块)
├── trimgalore/
├── samblaster/
└── unicycler/
```

## 五、注意事项

### 5.1 模块引用路径更新

需要检查并更新 `workflows/circdna.nf` 中的模块引用路径：

**需要更新的路径示例：**

```groovy
// 旧路径（已删除）
include { CIRCEXPLORER2_PARSE } from './modules/local/circexplorer2/parse/main'

// 新路径
include { CIRCEXPLORER2_PARSE } from './modules/nf-core/circexplorer2/parse/main'
```

**可能需要更新的模块：**
- `CIRCEXPLORER2_PARSE` - 从 local 迁移到 nf-core
- `SAMBLASTER` - 从 local 迁移到 nf-core
- `CNVKIT_BATCH` - 从 local 迁移到 nf-core
- `CNVKIT_SEGMENT` - 从 local 迁移到 nf-core
- `SEQTK_SEQ` - 从 local 迁移到 nf-core
- `UNICYLER` - 从 local 迁移到 nf-core

### 5.2 输入输出接口变化

#### CIRCEXPLORER2_PARSE
- **旧版本**: 直接处理 BAM 文件
- **新版本**: 处理 fusion/junction 文件
- **建议**: 可能需要调整工作流逻辑

#### BWA_MEM
- **新增**: fasta 参考基因组输入（用于 CRAM 输出）
- **新增**: 多种输出格式支持 (BAM/CRAM/SAM)
- **建议**: 检查是否需要 CRAM 输出支持

#### MultiQC
- **变化**: 动态输入路径
- **建议**: 检查 MultiQC 调用是否需要调整

### 5.3 配置参数

- 检查容器引擎配置
- 验证资源分配参数
- 确认输出格式选项

## 六、后续步骤

### 6.1 立即执行

- [ ] 检查并更新 `workflows/circdna.nf` 中的模块引用路径
- [ ] 验证所有模块的输入输出接口
- [ ] 运行测试流程验证替换结果

### 6.2 测试验证

```bash
cd /Users/siyangming/nextflow_nf_core/circdna.nf
conda activate nextflow
nextflow run main.nf -profile test_local -resume
```

### 6.3 性能监控

- [ ] 监控替换后的资源使用情况
- [ ] 比较替换前后的运行时间
- [ ] 验证输出结果的一致性

### 6.4 文档更新

- [ ] 更新流程文档中的模块版本信息
- [ ] 更新配置文件中的模块引用
- [ ] 记录已知的接口变化

## 七、自定义模块迁移计划

根据用户要求，以下自定义模块需要迁移到 bio.nf 进行标准化：

| 模块 | 目标位置 | 优先级 | 说明 |
|------|---------|--------|------|
| `ampliconsuite` | bio.nf/modules/ | 高 | 需要 MOSEK 许可证 |
| `bedtools/sortedbam2bed` | bio.nf/modules/ | 中 | 自定义 BAM 到 BED 转换 |
| `bedtools/splitbam2bed` | bio.nf/modules/ | 中 | 自定义 split BAM 到 BED |
| `circlefinder` | bio.nf/modules/ | 高 | 核心环状 DNA 检测算法 |
| `circlemap/*` | bio.nf/modules/ | 高 | Circle-Map 完整工具链 |
| `getcircularreads` | bio.nf/modules/ | 中 | 从 Unicycler 提取环状读取 |
| `samplesheet_check` | bio.nf/modules/ | 中 | 自定义样本表验证 |

**迁移原则：**
- 在 bio.nf 中按 nf-core 标准格式构建
- 使用时拷贝到流程的 `modules/nf-core/` 目录
- 日常更新和维护放在 bio.nf

## 八、风险评估

| 风险 | 级别 | 缓解措施 |
|------|------|---------|
| 输入输出接口变化 | 中 | 充分测试，必要时调整流程逻辑 |
| 容器兼容性 | 低 | 使用标准容器，保持向后兼容 |
| 性能变化 | 低 | 监控资源使用，优化配置 |
| 依赖关系变化 | 中 | 检查模块间依赖，更新配置 |

## 九、相关文档

- **版本控制**: Git 提交历史记录了所有变更
- **替换前状态**: 通过 Git log 和 git diff 查看变更前状态
- **原始文档**: `CHANGES&FIX/20260623.md`
- **参考标准库**: `/Users/siyangming/nextflow_nf_core/modules/modules/`

## 十、错误修复记录

### 10.1 模块引用路径更新错误

**错误现象**:
```
Error workflows/circdna.nf:14:1: Invalid include source: '/Users/siyangming/nextflow_nf_core/circdna.nf/modules/local/bwa/mem/main.nf'
Error workflows/circdna.nf:30:1: Invalid include source: '/Users/siyangming/nextflow_nf_core/circdna.nf/modules/local/samblaster/main.nf'
Error workflows/circdna.nf:34:1: Invalid include source: '/Users/siyangming/nextflow_nf_core/circdna.nf/modules/local/circexplorer2/parse/main.nf'
Error workflows/circdna.nf:36:1: Invalid include source: '/Users/siyangming/nextflow_nf_core/circdna.nf/modules/local/unicycler/main.nf'
Error workflows/circdna.nf:37:1: Invalid include source: '/Users/siyangming/nextflow_nf_core/circdna.nf/modules/local/seqtk/seq/main.nf'
Error workflows/circdna.nf:40:1: Invalid include source: '/Users/siyangming/nextflow_nf_core/circdna.nf/modules/local/multiqc/main.nf'
```

**修复内容**:
更新了 `workflows/circdna.nf` 中的模块引用路径，从 `local` 迁移到 `nf-core`:

| 模块 | 旧路径 | 新路径 |
|------|--------|--------|
| `BWA_MEM` | `../modules/local/bwa/mem/main` | `../modules/nf-core/bwa/mem/main` |
| `SAMBLASTER` | `../modules/local/samblaster/main` | `../modules/nf-core/samblaster/main` |
| `CIRCEXPLORER2_PARSE` | `../modules/local/circexplorer2/parse/main` | `../modules/nf-core/circexplorer2/parse/main` |
| `UNICYCLER` | `../modules/local/unicycler/main` | `../modules/nf-core/unicycler/main` |
| `SEQTK_SEQ` | `../modules/local/seqtk/seq/main` | `../modules/nf-core/seqtk/seq/main` |
| `MINIMAP2_ALIGN` | `../modules/nf-core/minimap2/align/main.nf` | `../modules/nf-core/minimap2/align/main` |
| `MULTIQC` | `../modules/local/multiqc/main.nf` | `../modules/nf-core/multiqc/main` |

### 10.2 BWA_MEM 模块接口变化

**错误现象**:
```
Error workflows/circdna.nf:204:13: Incorrect number of call arguments, expected 4 but received 3
```

**原因**: nf-core 标准库的 BWA_MEM 模块新增了 fasta 参考基因组输入参数（用于 CRAM 输出支持）

**修复内容**:
```groovy
// 旧版本调用（3个参数）
BWA_MEM (
    ch_trimmed_reads,
    ch_bwa_index,
    channel.value(true)
)

// 新版本调用（4个参数）
BWA_MEM (
    ch_trimmed_reads,
    ch_bwa_index,
    ch_fasta_meta,
    channel.value(true)
)
```

**版本输出变化**:
```groovy
// 旧版本
ch_versions = ch_versions.mix(BWA_MEM.out.versions)

// 新版本（分离版本输出）
ch_versions = ch_versions.mix(BWA_MEM.out.versions_bwa)
ch_versions = ch_versions.mix(BWA_MEM.out.versions_samtools)
```

### 10.3 BAM_MARKDUPLICATES_PICARD 子工作流接口变化

**错误现象**:
```
Error subworkflows/nf-core/bam_markduplicates_picard/main.nf:20:5: Incorrect number of call arguments, expected 2 but received 3
```

**原因**: nf-core 标准库的 BAM_MARKDUPLICATES_PICARD 子工作流接口从 3 个参数变为 2 个参数

**修复内容**:
```groovy
// 旧版本调用（3个参数）
BAM_MARKDUPLICATES_PICARD (
    ch_bam_sorted,
    ch_fasta_meta,
    SAMTOOLS_FAIDX.out.fai.collect()
)

// 新版本调用（2个参数）
SAMTOOLS_FAIDX (
    ch_fasta_meta.map{ meta, fasta -> [meta, fasta, []] },
    channel.value(true)
)

BAM_MARKDUPLICATES_PICARD (
    ch_bam_sorted,
    ch_fasta_meta.join(SAMTOOLS_FAIDX.out.fai)
)
```

### 10.4 SAMTOOLS_FAIDX 模块接口变化

**原因**: nf-core 标准库的 SAMTOOLS_FAIDX 模块需要 fai 文件作为输入（即使为空）

**修复内容**:
```groovy
// 旧版本调用
SAMTOOLS_FAIDX (
    ch_fasta_meta,
    [ [id:''], [] ]
)

// 新版本调用
SAMTOOLS_FAIDX (
    ch_fasta_meta.map{ meta, fasta -> [meta, fasta, []] },
    channel.value(true)
)
```

### 10.5 CIRCEXPLORER2_PARSE 模块输入变化

**原因**: nf-core 标准库的 CIRCEXPLORER2_PARSE 模块现在接受 fusion/junction 文件而不是 BAM 文件

**影响**: 需要检查工作流中是否需要额外的预处理步骤来生成 fusion/junction 文件

**当前状态**: 暂时保持原有调用方式，需要进一步验证输入兼容性

### 10.6 MULTIQC 模块接口重大变化

**错误现象**:
```
Error workflows/circdna.nf:481:9: Incorrect number of call arguments, expected 1 but received 14
```

**原因**: nf-core 标准库的 MULTIQC 模块接口从多个独立参数变为单个 tuple 输入

**修复内容**:
```groovy
// 旧版本调用（14个参数）
MULTIQC (
    ch_multiqc_config,
    ch_multiqc_custom_config.collect().ifEmpty([]),
    CUSTOM_DUMPSOFTWAREVERSIONS.out.mqc_yml.collect(),
    ch_workflow_summary.collectFile(name: 'workflow_summary_mqc.yaml'),
    ch_fastqc_multiqc.collect{ it -> it[1] }.ifEmpty([]),
    ch_trimgalore_multiqc.collect{ it -> it[1] }.ifEmpty([]),
    ch_trimgalore_multiqc_log.collect{ it -> it[1] }.ifEmpty([]),
    ch_samtools_stats.collect{ it -> it[1] }.ifEmpty([]),
    ch_samtools_flagstat.collect{ it -> it[1] }.ifEmpty([]),
    ch_samtools_idxstats.collect{ it -> it[1] }.ifEmpty([]),
    ch_markduplicates_flagstat.collect{ it -> it[1] }.ifEmpty([]),
    ch_markduplicates_stats.collect{ it -> it[1] }.ifEmpty([]),
    ch_markduplicates_idxstats.collect{ it -> it[1] }.ifEmpty([]),
    ch_markduplicates_multiqc.collect{ it -> it[1] }.ifEmpty([]),
)

// 新版本调用（1个参数）
ch_multiqc_input = channel.value([
    [id: 'multiqc_report'],
    ch_multiqc_files.collect{ it -> it[1] }.ifEmpty([]),
    ch_multiqc_custom_config.collect().ifEmpty([]),
    file(params.multiqc_logo ?: 'null'),
    file(params.multiqc_replace_names ?: 'null'),
    file(params.multiqc_sample_names ?: 'null')
])

MULTIQC (ch_multiqc_input)
```

**新接口说明**:
```groovy
tuple val(meta), path(multiqc_files, stageAs: "?/*"), path(multiqc_config, stageAs: "?/*"), path(multiqc_logo), path(replace_names), path(sample_names)
```

### 10.7 SAMTOOLS_SORT 模块接口变化（2026-06-25）

**错误现象**:
```
Error workflows/circdna.nf:228:13: Incorrect number of call arguments, expected 3 but received 1
Error workflows/circdna.nf:298:17: Incorrect number of call arguments, expected 3 but received 1
Error workflows/circdna.nf:342:9: Incorrect number of call arguments, expected 3 but received 1
Error workflows/circdna.nf:374:9: Incorrect number of call arguments, expected 3 but received 1
Error workflows/circdna.nf:384:9: Incorrect number of call arguments, expected 3 but received 1
```

**原因**: nf-core 标准库的 SAMTOOLS_SORT 模块需要 3 个参数

**新接口**:
```groovy
tuple val(meta), path(bam)
tuple val(meta2), path(fasta), path(fai)
val index_format
```

**修复内容**: 更新了 5 处 SAMTOOLS_SORT 调用：

| 调用位置 | 模块别名 | 参数修复 |
|---------|---------|---------|
| 第 228 行 | `SAMTOOLS_SORT_BAM` | 添加 `ch_fasta_meta.join(SAMTOOLS_FAIDX.out.fai)` 和 `'bai'` |
| 第 302 行 | `SAMTOOLS_SORT_FILTERED` | 添加 `ch_fasta_meta.join(SAMTOOLS_FAIDX.out.fai)` 和 `'bai'` |
| 第 348 行 | `SAMTOOLS_SORT_QNAME_CF` | 添加 `ch_fasta_meta.join(SAMTOOLS_FAIDX.out.fai)` 和 `''` |
| 第 382 行 | `SAMTOOLS_SORT_QNAME_CM` | 添加 `ch_fasta_meta.join(SAMTOOLS_FAIDX.out.fai)` 和 `''` |
| 第 394 行 | `SAMTOOLS_SORT_RE` | 添加 `ch_fasta_meta.join(SAMTOOLS_FAIDX.out.fai)` 和 `'bai'` |

### 10.8 SAMTOOLS_VIEW 模块接口变化（2026-06-25）

**错误现象**:
```
Error workflows/circdna.nf:290:17: Incorrect number of call arguments, expected 5 but received 3
```

**原因**: nf-core 标准库的 SAMTOOLS_VIEW 模块需要 5 个参数

**新接口**:
```groovy
tuple val(meta), path(input), path(index)
tuple val(meta2), path(fasta), path(fai)
tuple val(meta3), path(qname)
tuple val(meta4), path(bed)
val index_format
```

**修复内容**:
```groovy
// 旧版本调用（3个参数）
SAMTOOLS_VIEW_FILTER (
    ch_bam_sorted.join(ch_bam_sorted_bai),
    ch_fasta_meta,
    []
)

// 新版本调用（5个参数）
SAMTOOLS_VIEW_FILTER (
    ch_bam_sorted.join(ch_bam_sorted_bai),
    ch_fasta_meta.join(SAMTOOLS_FAIDX.out.fai),
    channel.empty(),
    channel.empty(),
    []
)
```

### 10.9 MINIMAP2_ALIGN 模块接口变化（2026-06-25）

**错误现象**:
```
Error workflows/circdna.nf:451:9: Incorrect number of call arguments, expected 6 but received 5
```

**原因**: nf-core 标准库的 MINIMAP2_ALIGN 模块需要 6 个参数

**新接口**:
```groovy
tuple val(meta), path(reads)
tuple val(meta2), path(reference)
val bam_format
val bam_index_extension
val cigar_paf_format
val cigar_bam
```

**修复内容**:
```groovy
// 旧版本调用（5个参数）
MINIMAP2_ALIGN (
    ch_circular_fastq,
    ch_fasta_meta,
    false,
    false,
    false
)

// 新版本调用（6个参数）
MINIMAP2_ALIGN (
    ch_circular_fastq,
    ch_fasta_meta,
    false,
    '',
    false,
    false
)
```

### 10.10 清理未使用变量（2026-06-25）

**警告**:
```
Warn workflows/circdna.nf:97:9: Variable was declared but not used
def ch_multiqc_config = channel.fromPath("$projectDir/assets/multiqc_config.yml", checkIfExists: true)
```

**修复内容**: 移除了未使用的 `ch_multiqc_config` 变量

### 10.11 修复总结（更新）

| 错误类型 | 影响模块 | 修复状态 | 备注 |
|---------|---------|---------|------|
| 模块路径错误 | 7个模块 | ✅ 已修复 | 从 local 迁移到 nf-core |
| 参数数量错误 | BWA_MEM | ✅ 已修复 | 新增 fasta 输入 |
| 参数数量错误 | BAM_MARKDUPLICATES_PICARD | ✅ 已修复 | 从 3 个参数变为 2 个 |
| 参数数量错误 | SAMTOOLS_FAIDX | ✅ 已修复 | 需要空 fai 输入 |
| 参数数量错误 | SAMTOOLS_SORT（5处） | ✅ 已修复 | 新增 fasta_fai 和 index_format |
| 参数数量错误 | SAMTOOLS_VIEW | ✅ 已修复 | 新增 qname、bed 参数 |
| 参数数量错误 | MINIMAP2_ALIGN | ✅ 已修复 | 新增 bam_index_extension 参数 |
| 输入类型变化 | CIRCEXPLORER2_PARSE | ⚠️ 待验证 | fusion/junction vs BAM |
| 接口重大变化 | MULTIQC | ✅ 已修复 | 从多参数变为单 tuple |
| 代码清理 | ch_multiqc_config | ✅ 已修复 | 移除未使用变量 |

---

**更新日期**: 2026-06-25
**执行人**: AI Assistant
**状态**: ✅ 完成
