# 2026-07-24 修改记录

## 概述

本次修改主要完成 Phase 0 Bug 修复和 ecc_finder 第四引擎集成，使长读 eccDNA 分析流程基础稳定性达到可运行状态。版本从 v3.1.0 升级到 v3.2.0。

## 修改清单

### 1. Phase 0 Bug 修复

#### Bug 1: FLED_PIPELINE 输入不匹配
- **文件**: `subworkflows/local/long_read_mapping/main.nf`
- **问题**: `LONG_READ_MAPPING` emit 为 2 元组 `[val(meta), sorted.bam]`（无 bai），但 `FLED_PIPELINE` 的 `take` 声明为 3 元组 `[val(meta), bam, bai]`
- **修复**: 
  - 在 `LONG_READ_MAPPING` 中新增 `SAMTOOLS_INDEX` 调用生成 bai
  - 通过 `sorted_bam.join(bam_index, by: [0])` 合并为 3 元组
  - `FLED_PIPELINE` 的 `take` 保持 3 元组不变

#### Bug 2: FLED_PIPELINE channel.value() 误用
- **文件**: `subworkflows/local/fled_pipeline/main.nf`
- **问题**: `channel.value(genome_fasta)` 中 `genome_fasta` 是 channel 而非值，导致 `Argument 2-nd of method 'channel.value' cannot be a channel object` 错误
- **修复**: 改用 `mapped_reads.combine(genome_fasta)` 将参考基因组合并到输入 channel

#### Bug 3: LONG_READ_PREPROCESSING emit 缺失
- **文件**: `subworkflows/local/long_read_preprocessing/main.nf`
- **问题**: 
  - `preprocessed_fastq` 在 `if` 块内用 `def` 声明，emit 时无法访问
  - `nanoplot_qc` 和 `versions` 的 emit 名称与变量名不匹配
- **修复**: 
  - 移除 `def` 关键字，使变量提升为 workflow 作用域
  - 使用 `emit: preprocessed_fastq = preprocessed_fastq` 语法显式绑定

#### Bug 4: LONG_READ_FILTERING 重复调用
- **文件**: `workflows/circdna.nf`
- **问题**: `LONG_READ_FILTERING` 被 CReSIL 和 FLED 分支分别调用，DSL2 中同名组件不能重复使用
- **修复**: 使用 `as` 别名引入两次：
  ```groovy
  include { LONG_READ_FILTERING as LONG_READ_FILTERING_CRESIL } from '...'
  include { LONG_READ_FILTERING as LONG_READ_FILTERING_FLED   } from '...'
  ```

#### Bug 5: circle_identifier 必填约束误伤长读模式
- **文件**: `nextflow_schema.json`, `workflows/circdna.nf`
- **问题**: schema 中 `circle_identifier` 标记为 required，但长读模式（pacbio/ont）不需要该参数，导致 stub 测试报错 `Missing required parameter: --circle_identifier`
- **修复**: 
  - schema 中移除 `circle_identifier` 的 required 约束
  - workflow 中将 circle_identifier 校验逻辑包裹在 `if (params.protocol == "short_read")` 内

### 2. ecc_finder 第四引擎集成

#### 2.1 模块迁移
- **来源**: bio.nf 新建分支构建
- **目标**: `modules/local/ecc_finder/{map_ont,map_sr,asm_ont,asm_sr}/main.nf`
- **容器**: `quay.io/bioinfortools/ecc_finder:1.0.0`（自构建上传至用户 quay.io 频道）

#### 2.2 子流程
- **文件**: `subworkflows/local/eccfinder_pipeline/main.nf`（新建）
- **功能**: 依据 `--eccfinder_mode`（map|asm|both）和 `--protocol`（pacbio/ont）选择子模块
- **输入**: `[val(meta), fastq]`, `[val(meta), genome_fasta]`
- **输出**: `eccdna_candidates`（CSV/FASTA）, `versions`

#### 2.3 主流程集成
- **文件**: `workflows/circdna.nf`
- **变更**: 在长读分支中新增 `run_eccfinder` 判断，调用 `ECCFINDER_PIPELINE`

### 3. Schema 更新

#### 3.1 新增参数
- `eccfinder_mode`: enum [map, asm, both]，默认 `map`

#### 3.2 参数调整
- `long_read_identifier`: 默认值从 `cresil,fled,flye` 改为 `cresil,fled,flye,eccfinder`
- `circle_identifier`: 移除 required 约束，长读模式下可留空

### 4. 测试配置

#### 4.1 新增测试 profile
- `conf/test_pacbio_lr.config`: PacBio 长读测试配置
- `conf/test_nanopore_lr.config`: Nanopore 长读测试配置
- 两者均添加 `process_high`/`process_medium`/`process_low` 资源上限，适配本地测试环境

#### 4.2 新增测试样本表
- `samplesheets/test_pacbio_lr.csv`
- `samplesheets/test_nanopore_lr.csv`

#### 4.3 nextflow.config 注册 profile
- 在 `profiles` 块中添加 `test_pacbio_lr` 和 `test_nanopore_lr`

### 5. FLED 模块 stub 补全
- **文件**: `modules/local/fled/main.nf`
- **变更**: 新增 `stub:` 块，生成空的 `${meta.id}.fled_circles.bed` 和 `${meta.id}.fled_report.txt`

### 6. 测试验证

#### PacBio 长读 stub 测试
```bash
NXF_OFFLINE=1 nextflow run . -stub -profile test_pacbio_lr --outdir results_test_pacbio_lr
```
- 状态: ✅ 通过
- 覆盖流程: INPUT_CHECK → NANOPLOT → (PBCCS/LIMA 跳过) → CRESIL_PIPELINE → FILTER_ECCDNA_BY_SUPPORT → LONG_READ_MAPPING → FLED_PIPELINE → FLYE_PIPELINE → MULTIQC

#### Nanopore 长读 stub 测试
```bash
NXF_OFFLINE=1 nextflow run . -stub -profile test_nanopore_lr --outdir results_test_nanopore_lr
```
- 状态: ✅ 通过
- 覆盖流程: INPUT_CHECK → NANOPLOT → (CHOPPER/PYCHOPPER 跳过) → CRESIL_PIPELINE → FILTER_ECCDNA_BY_SUPPORT → LONG_READ_MAPPING → FLED_PIPELINE → FLYE_PIPELINE → MULTIQC

## 待办事项

以下功能计划在后续版本实现：
- Phase 2: SV_DETECTION_PIPELINE（Sniffles2/cuteSV/SVIM）
- Phase 3: CIDERSEQ_PIPELINE（DeConcat/TideHunter/CircleSeeker）
- Phase 4: ECCDNA_ANNOTATION + UNIFIED_OUTPUT
- Phase 5: NanoPlot QC 集成到 MultiQC
- Phase 6: Metadata 分层系统
- Phase 7: 归档/删除 circdnalr.nf 目录

---

## 补充修改（2026-07-24 晚）

### 7. 临时文件清理
- **删除文件**:
  - `genome.txt`（目录列表捕获）
  - `program.txt`（unicycler 帮助输出）
  - `scripts/err`（nextflow log 捕获）
  - `scripts/errors.txt`（错误日志）
  - `scripts/reslts`（结果捕获，文件名拼写错误）
  - `scripts/Oryza_sativa_trace.txt`（trace 文件）
  - `scripts/命令.txt`（命令笔记）
  - `scripts/.nextflow.log`（nextflow 日志）
  - `results_test_nanopore_lr/`（stub 测试输出，可重新生成）
  - `results_test_pacbio_lr/`（stub 测试输出，可重新生成）
- **更新 `.gitignore`**: 新增 `results_test_*/`、`genome.txt`、`program.txt`、`scripts/err`、`scripts/errors.txt`、`scripts/reslts`、`scripts/*_trace.txt`、`scripts/命令.txt`、`*.log` 规则

### 8. conf/base.config 长读模块资源分配
- **文件**: `conf/base.config`
- **变更**: 新增 13 个 `withName` 资源覆盖块，为长读分析模块设置精确的 CPU/内存/时间配置
- **覆盖模块**:
  | 模块 | CPU | 内存 | 时间 | 说明 |
  |------|-----|------|------|------|
  | PBCCS | 12 | 48.GB | 24.h | PacBio HiFi 生成，CPU 密集 |
  | LIMA | 8 | 24.GB | 12.h | PacBio 引物去重 |
  | CHOPPER | 4 | 8.GB | 6.h | ONT 质量过滤 |
  | PYCHOPPER | 4 | 8.GB | 8.h | ONT 接头修剪 |
  | CRESIL_TRIM | 8 | 32.GB | 12.h | CReSIL 预处理 |
  | CRESIL_IDENTIFY | 12 | 64.GB | 48.h | eccDNA 检测，时间长 |
  | CRESIL_ANNOTATE | 8 | 32.GB | 24.h | eccDNA 注释 |
  | FLED | 8 | 32.GB | 24.h | 映射式 eccDNA 检测 |
  | FLYE | 12 | 100.GB | 96.h | 组装式检测，大基因组高内存 |
  | ECC_FINDER_MAP_* | 8 | 32.GB | 24.h | 映射式检测 |
  | ECC_FINDER_ASM_* | 12 | 64.GB | 48.h | 组装式检测 |
  | NANOPLOT | 2 | 8.GB | 6.h | 长读 QC |
  | FILTER_ECCDNA_BY_SUPPORT | 2 | 8.GB | 4.h | 检测后过滤 |
- **验证**: `nextflow config` 解析成功，所有 `withName` 块正确加载
