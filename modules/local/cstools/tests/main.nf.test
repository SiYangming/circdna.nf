nextflow_process {

    name "Test Process CSTOOLS"
    script "../main.nf"
    process "CSTOOLS"

    tag "modules"
    tag "modules_nfcore"
    tag "ciderseq"

    test("test-cstools-split") {

        options "-c ${projectDir}/modules/local/cstools/tests/nextflow.config"

        when {
            process {
                """
                input[0] = [ [ id:'example1' ], // meta
                             file("\${projectDir}/modules/local/ciderseq/testdata/example1.fastq", checkIfExists: true) ]
                input[1] = file("\${projectDir}/modules/local/ciderseq/testdata/ciderseq_config.json", checkIfExists: true)
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out.output, process.out.versions).match() }
            )
        }

    }

}
