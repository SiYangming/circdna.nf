docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g) --platform linux/amd64'
    fixOwnership = true
}

process {
    withName: ECC_FINDER_ASM_SR {
        ext.args = ''
        cpus = 2
    }
}
