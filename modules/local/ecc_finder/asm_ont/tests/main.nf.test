nextflow_process {

    name "Test Process ECC_FINDER_ASM_ONT"
    script "../main.nf"
    process "ECC_FINDER_ASM_ONT"

    tag "modules"
    tag "modules_nfcore"
    tag "ecc_finder"
    tag "ecc_finder/asm_ont"

    test("test-asm-ont - stub") {

        options "-stub -c ${projectDir}/modules/local/ecc_finder/asm_ont/tests/nextflow.config"

        when {
            process {
                """
                input[0] = [ [ id:'test' ], // meta
                             file("\${projectDir}/modules/local/ecc_finder/testdata/Arabi.ONT.1kReads.fasta.gz", checkIfExists: true) ]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

}
