nextflow_process {

    name "Test Process CIDERSEQ_PHASE"
    script "../main.nf"
    process "CIDERSEQ_PHASE"

    tag "modules"
    tag "modules_nfcore"
    tag "ciderseq"

    test("test-ciderseq-phase") {

        options "-c ${projectDir}/modules/local/ciderseq/phase/tests/nextflow.config"

        when {
            process {
                """
                input[0] = [ [ id:'example1', genome:'EACMV_DNA_A' ], // meta
                             file("\${projectDir}/modules/local/ciderseq/testdata/deconcat/example1.EACMV_DNA_A.fa", checkIfExists: true) ]
                input[1] = file("\${projectDir}/modules/local/ciderseq/testdata/annotate/example1.EACMV_DNA_A.json", checkIfExists: true)
                input[2] = file("\${projectDir}/modules/local/ciderseq/testdata/ciderseq_config.json", checkIfExists: true)
                input[3] = 'EACMV_DNA_A'
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out.phased, process.out.versions).match() }
            )
        }

    }

}
