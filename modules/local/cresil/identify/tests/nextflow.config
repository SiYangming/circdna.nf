docker {
    enabled = true
    runOptions = '--platform linux/amd64 -u $(id -u):$(id -g)'
}

process {
    withName: CRESIL_IDENTIFY {
        ext.args = '-s -break 1 -depth 1'
        cpus = 4
    }
}
