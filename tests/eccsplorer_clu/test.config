process {
    // subsample for a quick end-to-end validation run
    withName: 'ECCSPLORER_CLU_PREPARE' { ext.args = '--read-count 5000' }
    // lower candidate threshold for small subsampled test data
    withName: 'ECCSPLORER_CLU_CANDIDATES' { ext.args = '--ecc-prop 0.5' }
}
